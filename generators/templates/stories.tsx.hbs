import {{pascalCase name}} from '.'
import { Meta, StoryFn } from '@storybook/react'

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
  args: {
    title: '',
    description: ''
  }
} as Meta

export const Basic: StoryFn = (args) => <{{pascalCase name}} {...args} />
